export type Policy = {
  active: boolean;
  category: string;
  data_type: string;
  description: string;
  id: string;
  name: string;
  value: string;
};

export type updatePolicy = {
  value: string;
};
